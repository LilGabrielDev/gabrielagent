services:
  whatsapp-service:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-4000}:4000"
    env_file:
      - .env
    environment:
      PORT: 4000
      SESSION_PATH: /app/sessions
    volumes:
      - whatsapp_sessions:/app/sessions

volumes:
  whatsapp_sessions:

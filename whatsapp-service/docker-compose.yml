services:
  whatsapp-service:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      PORT: 3000
      SESSION_PATH: /app/sessions
    volumes:
      - whatsapp_sessions:/app/sessions

volumes:
  whatsapp_sessions:
